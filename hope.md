ROI 抽出
以前顕微鏡CCDで撮影したサンプルについて各プロジェクトごとに単一画像ファイル、zstack複数画像ファイル（のちにマージして単一画像ファイルへ加える）をアップロードしまとめて、ROI抽出を行なって推論→細胞カウント、適宜データベース化（データベースへ追加するボタンを導入（プロジェクト選択画面で（DB追加済みは明記する）
リアルタイムエンジン（本APIの真髄）
顕微鏡で,リアルタイムで観察しながら、CCDカメラで撮影し、画像をAPIへ転送し送られた画像をそのままROI抽出、推論（Deepscan）実際に観察して出た結果と推論の結果が違う場合Manual labelでアノテーションを行う(Class0,1,2,3)。ROI検出がされていない場合は、自分で手動ROIを作りアノテーションを行う。推論、（+アノテーション）されたサンプルについて利用する場合は保存ボタンを押し、作成したプロジェクトへ単一画像または、zstack複数画像ファイルとして保存し、次の視野の画像が送られたら、同じことを行う。基本的に画像の名前はシンプルに番号だけで001,002,003,004,005,…010,011…となるようにする。撮影途中で、この視野は凸凹で何枚かz軸を変えて撮影し、tenengradでマージしたものをその視野の単一画像として保存したいとなる時がある。その時に同視野zstackボタンをonにする。すると自動的に複数画像ファイル保存モードに切り替わる。そして、何枚か画像を撮影し、他の視野をとりたいとなったらボタンをオフにする。そうすると、単一画像保存モードとなる。このボタンの切り替を行なっても保存する画像の名前の番号が連続的にすることは変わりない。しかし、zstackボタンをonにしている時は同一視野の認識であるため、おくっている番号にさらに_001…を付け足す。要は、001,002までは単一画像で良くて、003での視野はzstackでとりたいとなった時、ボタンを押したら003_001となり、onのままとったら、003_002とoffになるまでこの形式は続く。その視野で撮り終えてoffにしたら次は004,005…と先頭の番号が続いていく。
データベース
各プロジェクトファイルを.db形式（この時、アップロード由来か、リアルタイムエンジン由来か明記してある）で保存してあり、開くと展開され、単一画像ファイルと複数画像ファイルがリスト化されている画面へ遷移する（ROI抽出の画面表示形式と同じ（パンクズは別にする）。そこで適宜複数画像ファイル(zstackファイル)をマージして単一画像ファイルへ保存して、単一画像すべてのROI 抽出、推論を一括で行うことにより、細胞数とROI数、全画像の各ROIについての画像をリスト化したページで結果を表示する（作成済み）
ホームの形式は今、ROI抽出、データベース、モデル選択、リアルタイムエンジンとなっているが、ROI抽出をファイルをアップロードという名前に変更。そして、HomeはROI抽出、データベース、モデルの選択の３つにし、ROI抽出を選択すると、ファイルをアップロード、リアルタイムエンジンの２つの選択肢が出てきて、それぞれ選択するように階層を１つ増やすようにする。
